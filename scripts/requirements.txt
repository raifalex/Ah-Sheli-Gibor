# Optional dependencies for tool-assisted validation.
# Regex-only mode (--no-model) requires no dependencies beyond Python stdlib.
#
# To enable DictaBERT-based checks:
#   pip install -r requirements.txt
#
# DictaBERT model (~800MB) will be downloaded automatically on first run
# from https://huggingface.co/dicta-il/dictabert-parse (CC BY 4.0).

transformers>=4.40.0
torch>=2.0.0
sentencepiece>=0.1.99
